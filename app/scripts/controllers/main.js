'use strict';

/**
 * @ngdoc function
 * @name slnApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the slnApp
 */
angular.module('slnApp')
  .controller('MainCtrl', function ($scope) {
  	$scope.background_img = "images/solar_cells.png"

  		// var form_data = $scope.form
  		// console.log(form_data);
  		// console.log(form_data.split(/\r\n|\n/));
  
  });
